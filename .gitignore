# Rust's output directory
target
.DS_Store
# Local settings
.soroban
.stellar
.env
# Private runbooks — keep local only
UPGRADE.md
fix.md

# Property-test snapshots are non-deterministic (random inputs each run), so they
# churn on every run and should not be committed.
contracts/accord/test_snapshots/test/approval_count_never_exceeds_owner_count.*.json